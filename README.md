# Dotfiles

```
cd ~
git clone git@github.com:krag3/d.git
cd d

make

# re-deploy dotfiles
make init

# re-install packages
make install

# additional shell utils
make install-shell-util

# remove symlinks
make clean
```